############################################################
# Ejemplo de variables de entorno (.env)                   #
# Copia este archivo a .env y rellena los valores necesarios#
############################################################

# ----------------------------------------------------------
# Ollama (inferencia local)
# URL base del daemon de Ollama
OLLAMA_BASE_URL=http://localhost:11434

# ----------------------------------------------------------
# Proveedores de Modelos Externos (OPCIONAL)
# NOTA: Normalmente las llaves se guardan de forma segura con keytar
#       desde la interfaz de "Models". Estas variables solo sirven
#       como referencia o para inicializaciones alternativas.
#       (Déjalas vacías o elimina si no las usas.)
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
COHERE_API_KEY=
MISTRAL_API_KEY=
GOOGLE_API_KEY=
# Si usas un endpoint OpenAI compatible personalizado
CUSTOM_OPENAI_BASE_URL=

# GitHub / Copilot
# Usado por el MCP de GitHub y/o integraciones que consumen la API
GITHUB_TOKEN=your_github_token_here

# ----------------------------------------------------------
# Integraciones de Búsqueda / Web
# Brave Search (para herramientas de búsqueda web)
BRAVE_API_KEY=your_brave_api_key_here

# ----------------------------------------------------------
# LinkedIn (OAuth2 para servidor MCP linkedin)
LINKEDIN_CLIENT_ID=
LINKEDIN_CLIENT_SECRET=
LINKEDIN_REDIRECT_URI=http://localhost:5173/linkedin-oauth-callback
# Scopes separados por espacio; mínimo para post de texto: r_liteprofile w_member_social
LINKEDIN_SCOPES=r_liteprofile w_member_social
# Una vez obtenido (linkedin_exchange_code) puedes colocar temporalmente aquí el access token
LINKEDIN_ACCESS_TOKEN=

# ----------------------------------------------------------
# Bases de datos / Persistencia (para MCPs relacionados)
POSTGRES_CONNECTION_STRING=postgresql://user:password@localhost:5432/database

# ----------------------------------------------------------
# Ajustes Generales de MCP
MCP_LOG_LEVEL=info          # trace | debug | info | warn | error
MCP_TIMEOUT=30000           # ms para timeout de llamadas tool
MCP_MAX_CONCURRENT_TOOLS=5  # límite de herramientas simultáneas

# ----------------------------------------------------------
# Placeholders para futuras integraciones (ejemplo)
# AZURE_OPENAI_API_KEY=
# AZURE_OPENAI_ENDPOINT=
# PERPLEXITY_API_KEY=
# SERPAPI_KEY=

# Fin del archivo
