{
  "version": "1.0.0",
  "description": "Simplified MCP configuration for Ollama Chat",
  "builtin_tools": {
    "filesystem": {
      "enabled": true,
  "description": "Built-in file system tools",
      "tools": ["list_dir", "read_file", "write_file", "path_info"]
    }
  },
  "external_servers": {
    "filesystem-external": {
      "type": "stdio",
      "command": "node",
      "args": ["-e", "console.log('MCP Server demo')"],
  "description": "External file server",
      "category": "core",
      "enabled": false
    }
  },
  "quick_start": {
    "enabled_by_default": ["filesystem"],
    "recommended_for_development": ["filesystem"]
  }
}
